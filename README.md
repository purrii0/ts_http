# js_http

Minimal HTTP server in Node.js/TypeScript. Serves files from the current directory.

## Usage

```bash
npx tsx src/server.ts [port]
```

Default port is 3000.

## Features

- Directory listing
- Directory traversal protection
- Common MIME types
- Request logging
